# The VIE needs no API key. Claude runs through your own Claude Code login:
# install Claude Code, run `claude` once to log in, then `python3 -m vie --check`.
#
# Optional settings (uncomment to use):
# VIE_MODEL=sonnet
# VIE_CLAUDE_PATH=/full/path/to/claude
